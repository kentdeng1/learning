# pointer-demo

用"跑起来看输出"的方式，过一遍嵌入式 C 语言指针的核心知识点。

覆盖：指针的本质（地址+类型）、数组名与指针步长、值传递 vs 指针传递、const 三种位置、结构体指针、二级指针、函数指针/回调、寄存器访问 + volatile、空指针防御。

```bash
make       # 编译
make run   # 编译并运行
```

建议边跑边对照 [main.c](main.c) 里的分节注释看，输出和代码一一对应。
