# 2026-09-07

## STM32 DMA 讲解(M2 外设)

K 问 DMA 原理 + 怎么写程序 + 如何运用。交付:
- 讲解:DMA 数据流架构图(CPU 只配置/搬完 1 次中断)、Circular 双缓冲 ping-pong 图
- 代码:寄存器级 4 寄存器配置(F1 USART1 RX)、HAL 两个实战模板(UART DMA + IDLE 不定长接收、ADC 多通道 circular)
- 坑清单 12 条(外设侧 DMA 使能位、buffer 不能局部、IDLE 长度算法、CNDTR 上限、RS485 等 USART TC、Cache 一致性、H7 DTCM 不可 DMA 访问等)
- 笔记:`notes/stm32-dma.md`(含 F1/F4 架构差异、寄存器表、CubeMX 配置、面试问答)

判断标准已给 K:数据量大(>几十字节)或速率高(>几十 kHz)才上 DMA,别过度设计。

关联:承接 09-03 的 `notes/c-bitfield-macro-cheatsheet.md`(位操作/宏主线),DMA 笔记里引用了该笔记。
